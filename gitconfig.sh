
# Config git at global level that is user-specific. Applys to an operating system user.
git config --global user.name "Babak Nikanjam"
git config --global user.email "babak.nikanjam@gmail.com"
git config --global core.editor "code --wait"
git config --global -e
